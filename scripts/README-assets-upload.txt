Asset transport boundary for Queue 5:
- Simulation runs in CI without credentials.
- REST adapter is protocol-compatible and unit-tested.
- Production transport uses the pinned Cloudflare TypeScript SDK with the short-lived upload-session JWT.
- Wrangler is not a production fallback.
