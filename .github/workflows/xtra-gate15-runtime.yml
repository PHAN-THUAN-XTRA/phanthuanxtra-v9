name: Production Smoke Gate-15

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Gate-15 registration test
        run: |
          echo "GATE15_REGISTRATION=PASS"
          echo "REF=${GITHUB_REF}"
          echo "SHA=${GITHUB_SHA}"
